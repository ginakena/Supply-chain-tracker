{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Foundry smart contracts for the Supply Chain Tracker",
  "license": "MIT",
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "deploy:local": "forge script script/Deploy.s.sol:Deploy --rpc-url localhost --broadcast",
    "deploy:sepolia": "forge script script/Deploy.s.sol:Deploy --rpc-url sepolia --broadcast --verify"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
